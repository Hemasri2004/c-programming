#include <stdio.h>

int main() {
    int  n;
    double i, s, sum=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
       // s= 1/i;
    sum =  1/i +sum;
       // printf("%f\n",sum);
    }
    printf("%lf",sum);
    return 0;
}
