#include <stdio.h>

int main() {
    int n,m,rem,sum=0;
    scanf("%d",&n);
    m=n;
    while(n!=0){
        rem=n%10;
        sum=sum+rem*rem*rem;
        n=n/10;
    }
    if(sum==m)
    printf("It is a armstrong");
 else
 printf("It is not a armstrong");
    return 0;
}
